# 项目结构说明

## 📁 完整目录结构

```
NewWebpage/
├── public/                          # 静态资源
│   └── vite.svg
│
├── src/
│   ├── app/                         # 应用入口
│   │   ├── App.tsx                 # 根组件（Provider 配置）
│   │   └── router.tsx              # 路由配置
│   │
│   ├── pages/                       # 页面组件
│   │   ├── AuthPage/               # 认证页面
│   │   │   ├── LoginPage.tsx      # 登录页
│   │   │   └── CallbackPage.tsx   # OAuth 回调页
│   │   │
│   │   ├── SearchPage/             # 搜索页面
│   │   │   └── index.tsx          # 搜索主页
│   │   │
│   │   └── FollowsPage/            # 关注列表页面
│   │       └── index.tsx          # 关注列表主页
│   │
│   ├── features/                    # 功能模块（按业务划分）
│   │   └── auth/                   # 认证功能
│   │       ├── api/
│   │       │   └── authApi.ts     # 认证 API
│   │       └── hooks/
│   │           └── useAuth.ts     # 认证 Hook
│   │
│   ├── components/                  # 通用组件
│   │   ├── layout/                 # 布局组件
│   │   │   └── RootLayout.tsx     # 根布局
│   │   │
│   │   ├── ui/                     # UI 组件（shadcn/ui）
│   │   │   └── (待添加)
│   │   │
│   │   ├── icons/                  # 图标组件
│   │   │   └── DiscordIcon.tsx    # Discord 图标
│   │   │
│   │   └── ProtectedRoute.tsx     # 路由守卫
│   │
│   ├── hooks/                       # 全局 Hooks
│   │   └── (待添加)
│   │
│   ├── lib/                         # 工具库
│   │   ├── api/
│   │   │   └── client.ts          # Axios 客户端
│   │   └── utils.ts               # 工具函数
│   │
│   ├── types/                       # 类型定义
│   │   └── env.d.ts               # 环境变量类型
│   │
│   ├── styles/                      # 样式文件
│   │   └── globals.css            # 全局样式
│   │
│   ├── assets/                      # 资源文件
│   │   └── (待添加)
│   │
│   └── main.tsx                     # 入口文件
│
├── .env.example                     # 环境变量示例
├── .eslintrc.cjs                   # ESLint 配置
├── .gitignore                      # Git 忽略文件
├── .prettierrc                     # Prettier 配置
├── index.html                      # HTML 模板
├── package.json                    # 项目配置
├── postcss.config.js              # PostCSS 配置
├── tailwind.config.js             # Tailwind 配置
├── tsconfig.json                  # TypeScript 配置
├── tsconfig.node.json             # Node TypeScript 配置
├── vite.config.ts                 # Vite 配置
├── README.md                       # 项目说明
└── PROJECT_STRUCTURE.md           # 本文件
```

## 🎯 核心文件说明

### 应用入口
- **src/main.tsx** - React 应用挂载点
- **src/app/App.tsx** - 根组件，配置全局 Provider
- **src/app/router.tsx** - 路由配置

### 认证流程
1. 用户访问 `/` → 路由守卫检查认证状态
2. 未登录 → 重定向到 `/login`
3. 点击登录按钮 → 跳转到后端 OAuth 授权
4. 授权成功 → 回调到 `/auth/callback?token=xxx`
5. 保存 token → 重定向到 `/`

### API 客户端
- **src/lib/api/client.ts** - Axios 实例配置
  - 自动添加 Authorization header
  - 401 自动跳转登录
  - 请求/响应拦截器

### 状态管理
- **TanStack Query** - 服务端状态（API 数据）
- **Zustand** - 客户端状态（待添加）

## 📝 开发指南

### 添加新页面
1. 在 `src/pages/` 创建页面文件夹
2. 在 `src/app/router.tsx` 添加路由
3. 如需认证，放在 `ProtectedRoute` 下

### 添加新功能模块
1. 在 `src/features/` 创建功能文件夹
2. 按照以下结构组织：
   ```
   features/
   └── feature-name/
       ├── components/    # 功能专用组件
       ├── hooks/        # 功能专用 Hooks
       ├── api/          # API 接口
       ├── store/        # 状态管理（如需要）
       ├── types/        # 类型定义
       └── utils/        # 工具函数
   ```

### 添加 UI 组件
使用 shadcn/ui CLI：
```bash
npx shadcn-ui@latest add button
npx shadcn-ui@latest add input
npx shadcn-ui@latest add dialog
```

组件会自动添加到 `src/components/ui/`

## 🔄 下一步开发计划

### 第一阶段：核心功能
1. ✅ 项目初始化
2. ✅ 认证系统
3. ⏳ 搜索页面布局
4. ⏳ API 集成

### 第二阶段：搜索功能
1. 搜索栏组件
2. 筛选器组件
3. 帖子卡片组件
4. 分页组件

### 第三阶段：高级功能
1. 关注系统
2. Banner 轮播
3. 标签系统
4. 虚拟滚动

### 第四阶段：优化
1. 移动端适配
2. 性能优化
3. 可访问性
4. 测试

## 💡 最佳实践

### 组件设计
- 保持组件小而专注
- 使用 TypeScript 类型
- 提取可复用逻辑到 Hooks
- 使用 Tailwind CSS 类名

### 状态管理
- 服务端状态用 TanStack Query
- 客户端状态用 Zustand
- URL 状态用 React Router

### 性能优化
- 使用 React.memo 避免不必要的重渲染
- 使用 useMemo/useCallback 缓存计算结果
- 图片懒加载
- 代码分割

## 🐛 常见问题

### 1. 路径别名不生效
确保 `tsconfig.json` 和 `vite.config.ts` 都配置了路径别名。

### 2. Tailwind 样式不生效
检查 `tailwind.config.js` 的 content 配置是否包含所有文件。

### 3. API 请求 401
检查 token 是否正确保存在 localStorage。

### 4. 环境变量不生效
确保变量名以 `VITE_` 开头，并重启开发服务器。
